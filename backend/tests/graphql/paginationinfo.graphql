# Tests GraphQL pour PaginationInfo
# Generes automatiquement le 2025-05-28 09:42:02
# Auteur: WalidBenTouhami

# Ce type est generalement utilise comme partie d'autres types plutôt que directement requete
# Exemple d'utilisation dans une requete paginee:
query GetPaginatedResults {
  paginatedResults {
    items {
      _id
      # autres champs...
    }
    pagination {
      totalItems
      totalPages
      currentPage
      pageSize
      hasNextPage
      hasPreviousPage
    }
  }
}
