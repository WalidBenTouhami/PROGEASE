# Disable mongodb-memory-server postinstall to prevent firewall blocks
# This prevents the package from trying to download MongoDB binaries from fastdl.mongodb.org
# which may be blocked by firewall rules in CI/CD environments
MONGOMS_DISABLE_POSTINSTALL=1
MONGOMS_SKIP_DOWNLOAD=1

# Legacy peer deps flag for compatibility
legacy-peer-deps=true
